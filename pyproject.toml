[project]
name = "teachops-demo"
version = "0.1.0"
description = "TeachOps deterministic demo pipeline"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "python-pptx>=1.0.2",
]
