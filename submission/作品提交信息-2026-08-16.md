# GOAI 初赛作品提交信息（照此填表）

> 生成：2026-08-16（依据官方提交页面截图回填）
> 附件 zip 大小、GitHub 提交号等运行时信息见文末"打包记录"，提交前已实际生成。

## 1. 表单逐字段填写内容

| 表单字段 | 填写内容 |
| --- | --- |
| 作品名称 * | `研序 TeachOps：多 Agent 教学设计预审与质量治理系统` |
| 代码仓库 | `https://github.com/qrx-joe/TeachOps` |
| Demo 链接 | 留空（初赛无在线 Demo；本地可复现运行方式见仓库 README §5，live 运行证据见 `docs/运行证据索引.md`） |
| 作品附件（zip）* | 选择 `submission/TeachOps_GOAI_初赛作品附件.zip`（单文件 ≤1200MB 限制，实际远小于） |
| 所在高校/单位 * | `独立开发` |
| 收件人姓名 * / 电话 * / 地址 * | **用户自填**（寄奖品用） |
| 短袖尺码（3 件） | **用户自填** |

## 2. 成员信息（每人：所在单位 + 职务/身份）

按 2026-08-16 提交页面已填写内容记录，提交前请再核对一遍：

| 成员 | 所在单位 | 职务/身份 |
| --- | --- | --- |
| 成员 1（本人） | 独立开发 | 独立开发 |
| 成员 2 | 江南大学 | 产品经理 |
| 成员 3 | 上海智铛医诺供应链有限公司 | 程序员 |

## 3. 附件 zip 内容说明（评审视角）

`TeachOps_GOAI_初赛作品附件.zip` = GitHub 公开仓库同一版本的完整快照（`git archive HEAD` 生成，与仓库链接内容一致），关键材料入口：

- `README.md` — 项目总览、运行方式、证据类型边界
- `submission/作品简介.txt` — 499 字作品简介（≤500 字）
- `submission/TeachOps_GOAI_初赛方案.pptx`（10 页，含 3 张 live 截图）/ `submission/TeachOps_GOAI_初赛方案.pdf` — 初赛方案
- `agents/` 四个 Agent Identity · `skills/` 三个 Skill 契约 · `demo/` 两套样例 · `src/` + `tests/` 确定性闭环 · `evidence/` live 运行证据 · `docs/` 文档

不含（已在 .gitignore 排除并经预检扫描确认）：API Key、`.env`、`evidence/private/` 原始截图、官方模板、内部 docx、虚拟环境。

## 4. 提交规则提醒

- **每赛段最多提交 3 次**；截止前**最后一次成功提交**为评审版本。
- 建议先完整提交一次锁定版本；若需修改，剩余额度内覆盖提交。
- 提交成功后重新进入作品页确认状态，回执截图存 `submission/receipts/`（不入库）。

## 5. 打包说明

- 生成方式：`git archive --format=zip -o submission/TeachOps_GOAI_初赛作品附件.zip HEAD`——zip 内容与 GitHub 仓库（`main` 分支推送后）**完全同一版本**。
- zip 大小与对应提交号：提交完成后记录到 `submission/receipts/`（本地留存，不入库）。
